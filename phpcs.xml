<?xml version="1.0"?>
<ruleset name="YHT WordPress Plugin Standards">
    <description>Coding standards for Your Hidden Trip Planner plugin</description>

    <!-- Only check PHP files -->
    <arg name="extensions" value="php"/>
    
    <!-- Show progress and sniff names -->
    <arg value="p"/>
    <arg name="colors"/>
    
    <!-- Exclude directories -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/languages/*</exclude-pattern>
    
    <!-- WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Relax some rules for better developer experience -->
        <exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
        <exclude name="Squiz.Commenting.FileComment.Missing"/>
        <exclude name="Squiz.Commenting.ClassComment.Missing"/>
        <exclude name="Generic.Commenting.DocComment.MissingShort"/>
        <exclude name="WordPress.NamingConventions.ValidVariableName.UsedPropertyNotSnakeCase"/>
        <exclude name="WordPress.Arrays.MultipleStatementAlignment.DoubleArrowNotAligned"/>
    </rule>

    <!-- Allow short array syntax -->
    <rule ref="Generic.Arrays.DisallowShortArraySyntax.Found">
        <severity>0</severity>
    </rule>

    <!-- Encourage short array syntax -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax.Found"/>

    <!-- Security Rules -->
    <rule ref="WordPress.Security.EscapeOutput"/>
    <rule ref="WordPress.Security.NonceVerification"/>
    <rule ref="WordPress.Security.ValidatedSanitizedInput"/>
    
    <!-- Database Rules -->
    <rule ref="WordPress.DB.DirectDatabaseQuery"/>
    <rule ref="WordPress.DB.PreparedSQL"/>
    
    <!-- WP.org Plugin Directory Rules -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array" value="your-hidden-trip"/>
        </properties>
    </rule>
    
    <rule ref="WordPress.WP.DeprecatedFunctions"/>
    <rule ref="WordPress.WP.DeprecatedClasses"/>
    <rule ref="WordPress.WP.DeprecatedParameters"/>
    
    <!-- Performance Rules -->
    <rule ref="WordPress.DB.SlowDBQuery"/>
    
    <!-- Check for PHP version compatibility -->
    <config name="testVersion" value="7.4-"/>
    <rule ref="PHPCompatibilityWP"/>
</ruleset>